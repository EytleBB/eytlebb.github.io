# 2026-09-22 四项更新整合发布

发布基线：`7b6a7e0`。本地回滚引用：`backup/pre-release-20260922`。

## 合并内容

- 首页布局与直白三语文案：`41b1060`，含 `56c8e63`。
- 展馆移动、视角及帧调度：`f5687cd`，含 `b4bb7d4`。
- 铭牌取名、投票、评论及服务：`5747f77`，含 `ad7a436`；零票名称立即生效，无名称显示 `NULL`。
- 展馆音乐音量：`29e7735`，由 0.42 调整为 0.70。

重叠文件逐段整合：保留新版移动和按键映射；读铭牌时停止移动、释放鼠标，关闭后即使鼠标锁定失败也完成镜头回程并显示继续入口；暂停场景收到标题更新时补绘。新增两项交互/帧调度回归测试。旧铭牌文案补丁已标注过时，避免覆盖最新规则。

## 发布前验证

- Node 完整测试：119 项通过。
- Python HTTP/SQLite 测试：22 项通过。
- 浏览器：首页双栏、真实最新日志与精选图片正常；展馆自动漫游完整加载，控制台无错误或警告。
- 浏览器：用实际留言组件和本地 API 提交名称成功，实时更新为 0 票的当前名称。
- 自动化鼠标锁定受浏览器环境限制，真实按键/铭牌关闭回程用执行实际代码的测试覆盖；不将自动漫游当作完整人工游玩验收。

## 后端和回滚

配置见 `scripts/production/`。代码、密钥、数据库、备份均在公开网站目录外。Nginx 同源代理、请求限速，API 只监听本机；systemd 独立低权限账号运行。在线 SQLite 备份每次检查完整性，保留 14 个每日快照与 6 个月度快照。仅清理已过期的验证令牌和限流计数，不清理访客内容。

首次安装会将旧网站归档及旧 Nginx、部署排除规则、服务配置保存在 `/srv/eytle-site/backups/pre-release-<UTC时间>/`。最终版本一致性、线上 API 与备份验收记录存放于服务器私有发布目录，不能上传至 `/var/www/eytle.cn`。
