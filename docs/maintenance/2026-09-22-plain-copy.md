# 2026-09-22 全站文案与首页结构修订

用户要求将添加的花哨文案统一改成直接说明，并指出主页结构不如原来清晰。用户明确图片只修改旁边的文字与操作提示，不编辑图片或嵌入文字。

## 当前实现

- 首页恢复左侧最新日志与留言、右侧图片预览。删除单独的项目/工具分区和底部留言分区；所有栏目继续由顶部导航进入。
- 保留森林日夜主题和已经确认的一张大图、两张小图。图片完整显示，说明采用实际图库序号，不再替作品虚构名称。
- 栏目介绍、页脚、按钮、加载/错误状态和展馆操作说明改为直接描述内容或行为，中英韩同步。
- 移除装饰性栏目编号和重复标签。Minecraft 坐标工具去除结果和错误文案中的装饰符号，并明确结果是估算坐标及下界坐标换算。
- 留言成功提示为“留言已发送”。没有配置发送服务时保留输入并提示使用邮箱，不再声称留言已保存。

## 验证

- `node --test --test-reporter=spec --test-isolation=none tests/*.test.js`：79 项通过，0 项失败。
- `js/main.js`、`js/museum.js` 语法检查通过。
- 预览 `http://127.0.0.1:8032/#about`：390、768、1280 px 三种宽度分别检查中文、英文、韩文，无页面横向溢出；页脚三语正确。查看了桌面夜间两栏和手机白天单栏截图。
- 展馆入口查看了三语说明，中文加载完成；已有交互逻辑未更改。展馆的渲染和性能不属于本轮改动范围。
- 站点源码及测试的 `git -c core.whitespace=trailing-space,space-before-tab,cr-at-eol diff --check` 通过；`mc-calc.html` 保留原有 CRLF 行尾。维护目录内的 `.patch` 文件保留补丁格式要求的空白上下文行，单独以 `git apply --check` 验证。

## 待发布会话整合

本功能分支不推送、不合入 main、不部署。尚未合并的展馆移动和访客命名功能包含额外界面文字，其对应替换补丁及适用提交见 [plain-copy/README.md](plain-copy/README.md)。这些补丁未应用到当前首页预览，也未修改其他任务工作区；由发布会话整合对应功能时应用并核验。

本记录及补丁均为内部维护资料，继续由 `scripts/deploy-excludes.txt` 排除在公开网站之外。
