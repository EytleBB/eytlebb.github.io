# 首页与展馆升级同步 · 2026-09-12

## 内容与基线

两个实现任务均在仓库根目录工作，变更已自然汇合，无须跨分支合并。共同基线为 `5df346d0dee86c99b385c9f61c70e46d1fcfd991`。

- [电影感白桦林首页](2026-09-12-homepage-cinema.md)：顶部导航、日夜响应式 WebP、独立森林动效、自托管字体、三语版面及异步导航修正。
- [Nocturne 展馆](2026-09-12-museum-nocturne.md)：拱顶、石材和金属、平面反射、实体顶灯、对齐的光束与投影、泛光遮挡及分模块渲染。
- 一并纳入重装系统后的恢复说明、Linux/SSH 维护入口、更新后的架构说明和部署排除规则。

保留现有展品、预览、日志与下载文件；旧草稿仍在忽略的历史目录中。

## 发布前检查

- 全站 `node --test tests/*.test.js`：37 项通过，0 失败。
- 全部 `js/*.js` 语法检查通过；`git diff --check` 通过。
- 本地 HTTP 校验 23 个运行文件及数据索引，全部 200 且响应与磁盘逐字节相同。
- 实现任务已经完成桌面/窄屏、三语、日夜、日志/画廊交互，以及展馆自动漫游检查，细节与限制保留在各自记录中。本同步任务的浏览器控制未发现可用浏览器，因此没有把本轮 HTTP 检查表述成新的视觉验收。
- 发布前显式运行 `.githooks/pre-commit`，生成索引后纳入同一源码树。

## 本地 Git 对象修复

暂存检查发现 30 个零字节松散对象及 1 个失效的 Codex 内部检查点引用。已原样移入本地快照的 `git-repair/`，从现有工作文件重建 17 个被暂存区引用的 blob，并逐个核对对象 SHA；源码文件和网站主分支历史不变。字体许可仅去除行尾空格。

## 同步方式

使用已连接的 EytleBB GitHub 账号创建完整的 Git blob/tree/commit；核对远程源码树与本地暂存树一致，非强制更新 `main`。随后 fetch 同一提交到本地，确认工作文件仍与提交一致，再通过 `git push tencent main` 触发现有部署 hook。最终核对 GitHub、本地、服务器裸仓库和部署工作目录的提交，以及线上文件 SHA-256。

本机 HTTPS Git 写入凭据尚未配置；连接器同步不等于修复普通 `git push origin main` 的认证。腾讯云 SSH 密钥已经可用。

## 备份与部署排除

- 本地升级前快照：`/home/yeom/Documents/ChatGPT/.thisIsEytle-sync-20260912-164310/`，包含未提交文件归档、二进制 diff、状态和基线。
- 服务器旧线上全量归档及部署配置：`/srv/eytle-site/backups/pre-sync-20260912/`。
- `scripts/deploy-excludes.txt` 是排除规则的版本化副本，需同步到 `/srv/eytle-site/deploy-excludes.txt`。
- 旧线上 `AGENTS.md`、`CODEx_DEPLOY_GUIDE.md`、`scripts/`、`tests/` 移入上述服务器备份的 `public-internals/`。源码仓库保留，网站不再提供这些内部文件；字体许可随字体保留。

发布结果（实际提交、线上校验清单与时间）保存到上述本地快照目录的 `release-verification.json`，避免将自身提交哈希写进其所属提交。
