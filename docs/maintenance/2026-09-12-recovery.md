# 网站迁移与进度核对 · 2026-09-12

## 恢复结果

- 来源：`/run/media/yeom/Backup/eyt_web/`（PSSD 的 Backup 分区）。
- 当前工作目录：`/home/yeom/Documents/ChatGPT/thisIsEytle/`。
- 完整复制 921 个文件、268 个目录，共 1,398,445,778 字节（约 1.40 GB）。包含主仓库 Git 历史、隐藏目录、素材、截图、部署压缩包和旧工作副本。
- 复制完成后，对所有 921 个文件逐一比对 SHA-256，全部一致。随后仅在当前工作副本处理 Linux 兼容配置和下述文档；PSSD 的原始内容保留。
- 工作目录原有的空 Git 仓库已保存到 `/home/yeom/Documents/ChatGPT/.thisIsEytle-recovery-20260912/initial-empty.git`。

## 版本基线与网站进度

| 核对项 | 结果 |
| --- | --- |
| 本地主分支 | `main`，`5df346d0dee86c99b385c9f61c70e46d1fcfd991` |
| 最新提交 | 2026-08-17 00:16:00 +08:00，`Speed up home gallery scroll` |
| GitHub | 实际执行 `git fetch origin --prune`；`main...origin/main` 为 `0 0`，没有领先或落后的提交 |
| 腾讯云裸仓库与部署工作目录 | 通过共享 SSH 直接读取，两处版本均为 `5df346d0dee86c99b385c9f61c70e46d1fcfd991`；部署工作目录无未提交改动 |
| 线上公开文件 | 10 项均返回 HTTP 200，按 LF 统一换行后与恢复的文件内容完全一致 |
| Patch Log | 34 篇，最新日期为 2026-08-01 |
| 图画展览会 | 67 张原图及对应的 67 张 WebP 预览 |

线上核对范围为 `index.html`、`museum.html`、`mc-calc.html`、`css/style.css`、`css/museum.css`、`js/main.js`、`js/museum.js`、`logs/index.json`、`images/gallery/index.json`、`images/gallery-preview/index.json`。没有逐一下载核对线上原图、音频和下载包；本地这些文件已与 PSSD 完整校验。

最近的代码进度包括：首页画廊自动滚动与速度优化、3D 展馆纹理流式加载及行走卡顿优化、渐进显影灯箱、纸飞机留言按钮、森林动效和日志字体范围调整。网站采用左侧导航与主内容区布局，保留中英韩三语、日夜主题、日志、画廊、项目、工具与下载入口。

GitHub：[EytleBB/eytlebb.github.io](https://github.com/EytleBB/eytlebb.github.io)。线上：[eytle.cn](https://eytle.cn/)。正式部署沿用腾讯云 Nginx 与 `git push tencent main`，GitHub 的 `origin` 用于保存源码。

## 保留的旧进度

- `.claude/worktrees/pensive-robinson/`：已修复旧 `D:/eyt_web/...` 工作树路径。分支 `claude/pensive-robinson` 基于 `4cafaf1`（2026-01-03），是当前 `main` 的祖先，落后 113 个提交。其 `index.html` 有未提交的旧版 Patch Log 第三屏样式草稿（66 行新增、1 行删除），原样保留，没有合入当前站点。另存了补丁副本。
- `eyt_web_repo/`：2026-07-05 的旧仓库副本，HEAD 为 `4981136`，落后当前主分支 52 个提交。忽略换行符后没有内容差异，原样保留。
- `feat/3d-museum-gallery`：同样停留在 `4981136`，其提交已经包含在主分支历史中。
- `deploy/`、`_shots/`、`eytle-redesign-concept.html`、`eytle-redesign-spec.md`、`tomerge.html` 等旧部署包和设计文件均已保留。
- Git 对象检查通过。保留的 dangling commits 是 4—5 月的旧历史对象，没有清理。未发现晚于当前主分支的 dangling commit；没有 stash 条目。

## Linux 环境恢复与验证

1. 主仓库 40 个文件只存在 CRLF/LF 差异，逐个确认归一化后等于 HEAD，再恢复为提交中的 LF 内容；未覆盖任何实质内容改动。
2. 刷新 Windows 留下的索引状态，保留 `core.filemode=false`，设置本仓库 `core.autocrlf=input`、`core.ignorecase=false`、`core.hooksPath=.githooks`。
3. 根据原提交及部署说明恢复本仓库身份 `EytleBB <3035986089@qq.com>`，未修改全局 Git 身份。
4. `pre-commit` hook 在独立临时仓库中实际运行通过，能生成并暂存按名称排序的画廊索引和按日期倒序排列的日志索引。
5. 当前 Node.js 为 v24.21.0，Python 为 3.14.4，Pillow 为 12.1.1，已可使用项目维护脚本。
6. `node --test tests/*.test.js`：32 项通过、0 失败。主站、展馆、画廊重命名脚本的 JavaScript 语法检查与 hook shell 语法检查均通过。
7. 使用临时本地 HTTP 服务读取上述 10 个核心路径，均返回 200 且响应字节等于本地文件；此项不包含浏览器交互或 WebGL 渲染验证。

开始本地预览：

```bash
cd /home/yeom/Documents/ChatGPT/thisIsEytle
python3 -m http.server 8000 --bind 127.0.0.1
```

访问 `http://127.0.0.1:8000/`。本站没有构建步骤，不需要 `npm install`。新增日志时创建 `logs/YYYY-MM-DD.txt`，提交 hook 会生成索引；新增画廊图片使用现有重命名与预览脚本。不要手改三个自动生成的 `index.json`。

## 核对范围与后续维护

- SSH 访问已恢复：最初流量经本机 Mihomo TUN，在 banner 阶段超时；绑定实际 Wi-Fi 网卡后连通并完成密码认证，随后通过共享连接核对了服务器。最终配置已改为 `/home/yeom/.ssh/config` 中的持久别名 `eytle-server`，同样匹配服务器 IP。配置包括 `BindInterface wlp0s20f3`、专用密钥、`ControlMaster auto`、`ControlPersist 10m`；临时的 Git `core.sshCommand` 覆盖已移除。切换网卡时需更新 `BindInterface`。
- 密钥认证：本机生成专用 Ed25519 密钥 `/home/yeom/.ssh/id_ed25519_eytle_server`，只将公钥追加到服务器 `ubuntu` 用户的 `authorized_keys`。私钥权限 `600`，为支持非交互连接未设置额外口令，未进入项目目录或 Git。服务器已有公钥及密码登录方式保留，原公钥列表已备份。验证时明确禁用密码、交互认证、SSH agent 和连接共享，从全新连接成功完成公钥认证；持久别名登录与 `git ls-remote tencent` 也已通过。以后使用 `ssh eytle-server`，连接过期后可自动重新用密钥认证。
- 服务器核对结果：裸仓库 `main` 和部署目录 `HEAD` 都是 `5df346d`，部署目录无未提交改动；`/var/www/eytle.cn` 下的上述 10 个核心文件 SHA-256 与本地逐字节一致。`post-receive` hook 存在且有执行权限，此次没有触发部署。
- GitHub 公开 fetch 已通过；没有测试写入认证。GitHub Actions 公共 API 查询遇到限流，未确认 Actions 状态。
- 此次没有提交、推送或部署。网站运行文件与恢复基线一致；当前主工作区新增的差异只有本记录、`AGENTS.md` 与 `CODEx_DEPLOY_GUIDE.md` 的迁移说明。
- 原维护手册与早期计划仍有旧布局和旧操作描述，未勾选的历史计划不代表功能尚未实现；判断进度应结合当前代码、提交记录和测试。

校验清单、线上文件哈希、服务器核对结果 `server-verification.json`、共享连接记录 `ssh-control-session.json`、密钥配置记录 `ssh-key-setup.json`（仅路径、指纹和验证结果，无私钥内容）、测试输出、迁移前的 Git 配置与索引、旧草稿补丁均位于本机的 `/home/yeom/Documents/ChatGPT/.thisIsEytle-recovery-20260912/`。该目录在项目之外，不会被网站仓库的常规提交包含。
