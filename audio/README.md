# Pictures At An Exhibition music

Place the final track at `audio/museum.mp3`.

The exhibition streams this one looping MP3 through a shared Web Audio source, so
every spatial speaker stays on the same playback timeline. A track around one
minute long is fine; trim leading/trailing silence if the loop should feel
continuous.
